rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Authenticated users can read _and_ write anywhere
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
